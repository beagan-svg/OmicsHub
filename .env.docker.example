# Copy this file to .env.docker and fill in the required values.

# Use a long random string. Changing it signs out every user.
SECRET_KEY=

# PostgreSQL uses this password when it creates the database and when the app connects.
# Changing it after the volume exists does not change the stored PostgreSQL password.
POSTGRES_PASSWORD=

# Encrypt temporary AWS log-viewer credentials in Redis. Generate a unique Fernet key:
# docker run --rm python:3.12-slim python -c "import base64,secrets; print(base64.urlsafe_b64encode(secrets.token_bytes(32)).decode())"
CREDENTIAL_ENCRYPTION_KEY=

# Serve the local app over HTTP on port 8001. Set SECURE_SSL_REDIRECT=True and the secure
# cookie flags to True when an HTTPS proxy serves the app.
WEB_PORT=8001
SECURE_SSL_REDIRECT=False
SECURE_HSTS_SECONDS=0
SECURE_HSTS_INCLUDE_SUBDOMAINS=False
SECURE_HSTS_PRELOAD=False
SESSION_COOKIE_SECURE=False
CSRF_COOKIE_SECURE=False
CACHE_KEY_PREFIX=omicshub
CACHE_TIMEOUT=300
REDIS_BROKER_PORT=6379
REDIS_CACHE_PORT=6380

# Use the absolute path to the AWS credentials file. Compose does not expand ~.
AWS_CREDENTIALS_FILE=/opt/omicshub/aws/credentials

# Use the prod OCS account. The app reads and submits jobs in this account.
OCS_ENV_BASE=prod
OCS_AWS_REGION=us-west-2
# Use this AWS profile from AWS_CREDENTIALS_FILE.
AWS_PROFILE=omicshub
# Seconds to wait for one `ocs` CLI call before giving up. Optional, defaults to 300.
OCS_CLI_TIMEOUT=300

# Accept requests from localhost and the tunnel host.
ALLOWED_HOSTS=localhost,127.0.0.1
# Add the tunnel URL, including its scheme, to allow POST requests through the tunnel.
CSRF_TRUSTED_ORIGINS=
# Change this path before exposing the app beyond localhost.
ADMIN_URL=admin

# Optional observability settings.
SENTRY_DSN=
SENTRY_RELEASE=
GIT_SHA=
# The setup command overrides this with staging or production. This value is used when
# Docker Compose is run directly.
ENVIRONMENT=production
LOG_VIEWER_CREDENTIAL_TTL_SECONDS=28800
CONN_MAX_AGE=60
# Seconds between StageStatus sync sweeps. Optional, defaults to 300.
STAGE_STATUS_SYNC_SECONDS=300
